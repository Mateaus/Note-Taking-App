<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightbrown</item>
        <item name="colorPrimaryDark">@color/chocobrown</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditTextThemeNote" >
        <item name="colorControlNormal">@color/lightbrown</item>
        <item name="colorControlActivated">@color/brown</item>
        <item name="colorControlHighlight">@color/chocobrown</item>
    </style>

</resources>
